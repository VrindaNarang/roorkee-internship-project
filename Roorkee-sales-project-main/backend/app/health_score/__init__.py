# Customer Health Scoring engine (see PROJECT_SPEC.md section 6 / Milestone 6).
# config.py    - tunable thresholds and 0-100 score bands
# weights.py   - per-factor weights for the weighted scoring model
# scoring.py   - pure scoring math (normalization + weighted sum)
# health_service.py - DB access, recalculation, and read queries
